# --- Python Environment ---
# We don't upload the "engine" that runs the code, only the code itself
__pycache__/
.venv/
venv/
env/
*.py[cod]

# --- Database & Privacy (CRITICAL) ---
# This ensures your business.db stays on your local E: drive only
*.db
*.sqlite
data/

# --- Streamlit & System ---
# Local configuration and OS-specific clutter
.streamlit/
.DS_Store
Thumbs.db
